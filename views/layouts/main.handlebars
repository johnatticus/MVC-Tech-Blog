<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MVC Tech Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="flex-column min-100-vh">
  <nav class="navbar">
    <ul>
      <li class="li-inline nav-item"><a href="/">Home</a></li>
      <li class="li-inline nav-item"><a href="/dashboard">Dashboard</a></li>
      {{#if logged_in}}
      <li class="li-inline nav-item"><a href="#" id="logout-link">Logout</a></li>
      {{else}}
            <li class="li-inline nav-item"><a href="/login">Login</a></li>
      {{/if}}
    </ul>
  </nav>
  <header class="hero">
    <h1 class="app-title"><a href="/">MVC Tech Blog</a></h1>
  </header>

  <main class="col-auto">
    {{{body}}}
  </main>

  <footer class="flex-row justify-center">
    &copy; The Tech Blog Folks
  </footer>
</body>

</html>
<script src="/js/logout.js"></script>






{{!-- CREATE TABLE `user` (
  `first_name` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `last_name` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `email` VARCHAR(100) NOT NULL COLLATE 'utf8_unicode_ci',
  `password` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `gender` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `fitness_level` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `city` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `state` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci',
  `availability` VARCHAR(50) NOT NULL COLLATE 'utf8_unicode_ci'

)
COLLATE='utf8_unicode_ci'
ENGINE=InnoDB; --}}