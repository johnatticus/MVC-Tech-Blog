// Create the time stamps for the posts/comments here  format_date: (date) => {
// Format date as MM/DD/YYYY
module.exports = {
    format_date: (date) => {
      // Format date as MM/DD/YYYY
      return date.toLocaleDateString();
    }
};